includes:
    - phpstan-baseline.neon

parameters:

    paths:
        - app
        - bootstrap/app.php

    excludePaths:
        - database/migrations
        - database/seeders

    level: max

    tmpDir: /tmp/phpstan
